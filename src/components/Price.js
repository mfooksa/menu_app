const Price = (props) => {
  // console.log(Array.from(props.size_prices).length)
  return (
    <div>
      {/* for(let i = 0;i<props.size_prices.length;i++){
      <p>i</p>
    } */}
    </div>
  )
}

export default Price
